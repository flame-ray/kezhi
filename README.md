# 课织（Kezhi）

当前工作版本 **1.0.1**：教务导入网址默认留空，不预设任何学校；联系开发者入口移到设置页顶部并加强视觉提示。功能与升级说明见 [1.0.1 更新说明](docs/RELEASE_1.0.1.md)。实际已发布的版本和安装包请以 [GitHub Releases](https://github.com/flame-ray/kezhi/releases) 为准。

本版本包含此前的单次调课／停课及撤销、调课冲突确认、导入周次逐条核对、Android 浏览器导航，以及课表重影、长按时间选择和底部导入按钮修复。旧 v0.4.5 安装包不包含这些后续更新。

课织是一个面向 Windows 10/11 与 Android 的本地优先大学课表应用。0.4.4 版本统一了 Material 风格的亮暗主题、9 类弹窗与底部面板、日期跟手滑动和按钮反馈。支持弹窗退场、下拉收起、嵌套焦点恢复及系统减少动态效果。账号档案保存在本机 SQLite，密码独立存入 Windows Credential Manager 或由 Android Keystore 保护的密文。应用继续提供通用选课助手、成绩中心、周课表、每日安排、ICS 导入导出、单双周、多套作息与系统提醒。详见 [0.4.4 界面与动效验收](docs/UI_MOTION_AUDIT_0.4.4.md)。

首次启动默认是空课表，不附带任何虚构课程、学生或学校信息。用户完成教务导入或手动添加课程后才会创建课表数据。

## 本地运行

### Windows 一键启动

直接双击项目根目录的 [`启动课织.cmd`](./启动课织.cmd)。如果已经生成 `src-tauri/target/release/kezhi.exe`，脚本会立即启动原生版；否则会使用 Bun 进入 Tauri 开发模式并自动完成首次编译。

仓库当前已经生成可运行的 Release EXE 和未签名安装包；公开代码签名仍属于后续工作。

无需开发环境的安装程序可从 [GitHub Releases](https://github.com/flame-ray/kezhi/releases) 下载。当前安装包尚未配置公开代码签名证书，Windows 可能显示“未知发布者”。

### Android 开发版

Android 1.0.0 使用独立正式签名。旧测试版用户须先导出外部 JSON 备份，再卸载旧版、安装正式版并恢复；账号密码需重新保存。开发者签名步骤和密钥备份注意事项见 [正式签名说明](docs/ANDROID_RELEASE_SIGNING.md)。

项目已生成 Tauri Android 原生工程，支持 Android 7.0（API 24）及以上版本。Android Studio、SDK 35/36、NDK r28 和 Rust Android targets 配置完成后，可双击 `启动课织安卓版.cmd`；脚本会检测已连接的真机/模拟器，并启动 Android 调试版。

也可以使用命令：

```bash
bun run android:init
bun run android:dev
bun run android:build
```

Windows 上 Tauri 打包需要创建符号链接，因此必须在“设置 → 系统 → 开发者选项”中由用户明确开启开发人员模式。账号、课程和作息只保存在 Android 应用私有 SQLite 中；主分支的 Android 课表登录页使用底部大号导入按钮，具体模式与适配限制见待发布更新。用户选择“自动保存并填充”时，密码仅以 Android Keystore 保护的密文保存在应用私有目录。

### 开发模式

```bash
bun install
bun run dev
```

启动 Windows 桌面壳：

```bash
bun run tauri dev
```

如果 npm 依赖源暂时不可用，可以使用本机 Bun 对 React/TSX 源码进行独立构建验证：
- [x] Material 风格竖屏重排、五项底部导航与横排课程文字优化

```bash
bun build src/main.tsx --outdir dist-bun --target browser
```

## 设计原则

- 学号、姓名和课程数据默认只保存在本机。
- [x] 手机跟手滑动翻周、边界回弹与每日完整日期
- [x] 亮色/暗色主题切换与本地记忆
- [x] 长按空白课程格并预填星期、节次
- [x] 第 1 周周一可调整并随 SQLite、备份和日历导出保存
- [x] Android 侧滑和返回键按弹窗、向导步骤、详情页逐级返回，主界面不误退出
- [x] “今天”日程、逐日滑动、课程进行状态与返回周课表
- [x] 课程卡片自动显示单周 / 双周，不依赖手动备注
- [x] 通用选课页面学习器：表单、链接和同域网络资源候选识别
- [x] 多课程余量监控、最短 30 秒限频、失败退避与官方确认
- [x] 全自动定时抢课：服务器时钟校准、会话预热、贴近开放时刻触发、并发提交与抖动退避
- [x] 成绩录入、中文 CSV 导入、学期筛选和本地编辑
- [x] 加权平均分、GPA、已获学分、通过率和成绩分布
- [x] 大一校历独立适配：2026-09-17 周四作为第 1 周正式上课日，教务全校周次自动换算
- 学校登录与解析通过适配器接入，课表界面只消费统一数据模型。
- 本地修改与教务同步发生冲突时必须由用户确认。
- 动画尊重系统的“减少动画”设置。
- 插件能力采用声明权限和最小授权模型。

## 当前状态

- [x] Windows 桌面工程骨架
- [x] 七天周课表与课程详情
- [x] 空课表首次启动与导入引导
- [x] 周次切换动画
- [x] 拖放调整课程星期与节次
- [x] 多套作息编辑与本地保存
- [x] 作息方案可继续添加节次（最多 30 节），非法时间在写入前拦截
- [x] 新建、编辑和删除课程
- [x] 单次调课／停课、持久化撤销、今天/提醒/导出联动及时间冲突确认
- [x] 连续周、单双周和自定义周次
- [x] 多学校导入向导与官方页面安全登录说明
- [x] 宁德师范学院独立 WebView2 登录窗口
- [x] 多账号独立会话目录与本地账号备注
- [x] 正方课表 HTTPS 请求与 `kbList` 解析
- [x] 正方单双周、星期、连续节次与异常记录测试
- [x] 真实课表重新读取与同步差异逐项确认
- [x] 启动时会话恢复与前台定时自动检查
- [x] 规范化 SQLite 本地存储与旧数据迁移
- [x] 年级自动识别与大一/大二至大五校历切换
- [x] 相邻周轻量预览、纵向位置同步与无闪回翻页
- [x] 本地多账号档案迁移到 SQLite
- [x] Windows / Android 原生系统课程提醒
- [x] 全局默认提醒与单课程覆盖 / 关闭
- [x] 修改课表、首周日期和作息后自动重排未来提醒
- [x] Windows NSIS 当前用户安装包
- [x] Windows 双击一键启动
- [x] Android 原生工程与手机响应式界面
- [x] Android 系统返回手势应用内导航
- [x] 添加课程、ICS 导入、“今天”页面的 Android 返回手势层级
- [x] Android SQLite 多账号档案与旧数据迁移
- [x] Android SDK 36 / NDK r28 / 16 KB 页面对齐
- [x] Android 内置官方网页登录、明显导入按钮与手动同步
- [x] Android arm64 测试签名 APK / GitHub 发布
- [ ] Android 正式发行签名 / AAB 商店发布与后台自动同步
- [x] ICS 日历导出
- [x] ICS 日历导入、重复规则展开、作息节次匹配与合并 / 替换
- [x] CSV / Excel 兼容导出
- [x] JSON 本地备份
- [x] JSON 备份校验与恢复
- [x] SVG 高清课表图片
- [x] Windows 打印与 PDF 流程
- [ ] 使用真实学生会话完成宁德师院端到端验收
- [ ] 更多学校与更多版本正方系统适配器
- [ ] 账号退出、会话清理与 Windows 凭据管理界面
- [ ] 导入确认与同步差异页
- [ ] 托盘、小组件和插件宿主

## 全自动抢课

在「选课助手 → 第 5 步 全自动抢课」里配置一次，之后到点即可无人值守：

1. **学习入口**：第 1 步打开学校选课页，登录并**真的点一次「选课」按钮**（成功与否都行），再点「读取」。课织会记录同域的页面结构，并通过挂钩 `fetch` / `XMLHttpRequest` 自动捕获这次提交请求，直接生成第 5 步的提交接口与参数模板 —— 不需要自己开开发者工具抓包。
2. **添加课程**：第 2 步填入要抢的课程编号 / 名称。
3. **核对参数**：第 5 步点「把首门课的信息换成占位符」，把捕获到的课程 ID 换成 `{courseCode}` 等占位符，运行时会按每门课自动替换。也可用 `{courseName}`、`{teacher}`、`{timestamp}`。
4. **设定时间**：选择开抢时间与预热时长，点「立即校时」用学校服务器 `Date` 响应头校准本机时钟偏差。
5. **部署**：点「部署自动抢课」。应用重启后只要时间未过也会自动重新武装。

仍然需要人工参与的只有三处：**确认学校官网上的登录**（可由系统保险库保存并自动填充账号密码）、**点一次选课按钮供学习**、**填写候选课程清单**。其余流程按当前配置执行。

执行流程为：校时 → 提前预热登录会话 → 等待开放时刻 → 按并发数提交 → 抖动退避重试 → 抢到即停。标准 HTTP `Date` 响应头通常只有秒级精度，因此界面显示的毫秒数是本地计算单位，不代表学校服务器提供了毫秒级时间源。

内置的保护措施：

- 提交地址必须是用户学习过的同域 HTTPS 地址，原生层会拒绝退出登录与删除类路径；
- 并发上限 3、最短间隔 250 毫秒、每门课最多 200 次、整轮最长 30 分钟；
- 遇到验证码或登录过期立即停止，不做任何识别与绕过；
- 「立即试跑（会真实提交）」会立刻发送一次真实选课流程，但不会保存为定时任务；任务结束后会自动解除部署状态。

请把它当作替你守在电脑前的手，而不是攻击工具：频率请照顾学校服务器的承受能力。

## 登录隐私边界

- 用户可选择在课织中输入密码并保存；Windows 使用 Credential Manager，Android 使用 Keystore AES-GCM，密码不进入 SQLite、JSON 备份、日志或 Git。
- React 界面只能保存、查询状态和删除凭据，不能读回明文；原生层只向经过域名校验的学校官方登录页填充，且不会点击登录按钮。
- 自定义学校入口仅接受无内嵌账号密码的 HTTPS 网址。Android 通用课表模式跳转到新认证域名时，需要用户逐个核对确认；已保存密码只在原学校域名填充。Windows 和选课请求仍保留原有同域限制。
- Cookie 值只在 Rust 原生层用于本次学校请求，不返回 React 界面、不写入日志。
- 正方接口响应中的学生资料块会被丢弃，仅课程行进入本地解析器。
- 不提供云同步，课程、账号备注和系统保险库凭据仅留在当前设备。
- Windows 原生版使用 `%APPDATA%\app.kezhi.desktop\kezhi.sqlite3`，Android 使用应用私有数据库；SQLite 保存失败时会回退到兼容存储并提示用户。

## 同步行为

首次真实导入会保存所选学年和学期。应用启动时，如果距离上次同步已经超过 15 分钟，会尝试在后台恢复该账号会话；应用保持前台运行时每 30 分钟检查一次。新增、删除、教室、教师、节次和周次变化都会进入确认窗口，本地手动课程不会被学校同步删除。

## 课程提醒

设置页可开启系统课程通知并选择默认提前时间；课程详情可快速开关，课程编辑页可设置单独提前时间。课织会依据学期首周日期、课程周次和当前作息方案预定未来 128 条通知，并在相关数据变化或应用再次启动时自动补充。提醒设置随 SQLite 与 JSON 备份保存，ICS 导出会写入 `VALARM`。
