# 课织 1.2.2：楼栋地图与当前教学周

- 按用户要求取消校园导航：移除起点选择、位置点选、路线规划和道路计算模块。
- 保留点击教室高亮楼栋、手动选楼、放大缩小、全图与返回课程。
- 默认以最低缩放显示完整地图；需要查看细节时可主动放大或点击“回到楼栋”。
- 高亮采用蓝色圆角描边、白色衬边与柔和光晕；打开或切换楼栋时短暂浮现，不持续闪烁，尊重系统减少动态效果设置。
- 当前周启动、今天高亮与翻周优化保持不变。未修改用户课表数据、底部主导航或教务浏览器返回功能。
- 本次发行包含此前本地 1.2.0、1.2.1 的最终功能：课程教室地图、当前教学周启动、今天日期整列高亮、跨日刷新与翻周动画衔接。校园路线导航已取消。
- 旧安装包不变，需要安装新版生效。建议覆盖升级前导出 JSON 备份；Android 沿用原正式签名。

## 验证

- 222 项前端测试与 22 项 Rust 测试通过；移除导航模块时同步移除了其 4 项路由测试。
- 地图界面回归通过：默认全图、最低缩放、浮现动画、减少动态效果、切换楼栋、无起点/路线控件、点击地图不画路线、返回层级、窄屏和暗色主题。
- 当前周启动、今天高亮和翻周触控回归通过；没有连接 ADB 真机，尚未实机验证。

## 发行附件

- Kezhi-1.2.2-arm64.apk：22,431,504 字节，versionCode 10202；原正式签名 v2/v3 与 16 KB 对齐验证通过。
- Kezhi-1.2.2-windows-x64-setup.exe：6,144,300 字节；未配置 Windows Authenticode 签名，系统可能提示未知发布者。
- 两端新版 JS/CSS 和地图资源已核验。校验文件：RELEASE-1.2.2-SHA256SUMS.txt。旧安装包保留。
- 地图只适用于所提供的校园示意图，位置未经实地测绘核验，不含 GPS、路线或楼内楼层导航。
